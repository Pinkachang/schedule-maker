# TPBL 賽程排程工具 — 交接文件

給接手維護／修改本工具的人。假設你懂基本 HTML/CSS/JS，但不熟這個專案。

工具怎麼用見 `README.md`；本文著重**背景脈絡、資料怎麼運作、要改東西時去哪改、以及踩過的坑**。

---

## 1. 這個工具在整個排程流程中的位置

TPBL 一季 7 隊、每隊主場對每個對手 3 場，全季共 **7 × 6 × 3 = 126 場**。

- **整份賽程都是用這個工具排的**，包含主場骨架。實務上分兩階段：先鋪**主場骨架**（哪一隊哪個週末/平日在自己主場打，受球團回報的場館檔期白名單約束），再**指派客隊**（把對手填進既有的主場時段，同時滿足結構性、後勤與公平性條件）。
- 兩階段用的是同一個排賽介面：點格子時主場、客場一起選；「清除所有客隊」就是為了**保留主場骨架、只把客隊倒掉重排**而存在的。
- 工具**不自動求解**，是給人手動排＋即時檢查＋統計的輔助工具：你排，它即時告訴你哪裡撞了、分配平不平均。

---

## 2. 一分鐘看懂資料怎麼跑

- 所有狀態都在一個全域 `state` 物件裡（結構見 README）。
- 一場比賽就是 `state.matches['2026-11-07|19:00'] = { home:隊id, away:隊id, note:'選填備註' }`；`away` 可以是 `'TBD'` 表示客隊待訂，`note` 沒填就不寫這個欄位。
- 任何改動 `state` 的操作，結尾都會呼叫 `renderAll()`（重畫）和 `autoSave()`（去抖動寫 localStorage）。`renderAll()` 內部已含 `autoSave()`，不必再各自呼叫。
- **分頁是切過去才重畫的**：`switchTab()` 與 `renderAll()` 各有一張分派表，依當前分頁呼叫對應的 `renderXxx()`。所以你停在某頁、用別頁改了資料，得重新點一下該頁才會刷新。這是刻意的效能取捨，不是 bug。
- **唯一可攜的真實檔是 JSON**（💾 存檔）。localStorage 只綁單一瀏覽器，別當備份。

### ⚠️ 兩張分派表必須同步

```js
function switchTab(t){ ... if(t==='homecal')renderHomeCal(); }
function renderAll(){ ... if(currentTab==='homecal')renderHomeCal(); }
```

新增分頁時**兩張表都要加**。只加 `renderAll` → 點進該分頁是空白；只加 `switchTab` → 在該分頁改資料不會即時刷新。這兩個漏法都真的發生過。

---

## 3. 檔案內部地圖

單一 `index.html`，約 3,280 行，三段：

| 區塊 | 大致內容 |
|------|----------|
| `<style>`（前段） | 全部 CSS。含公平性表 `.fair-tbl`、季中分布 `.mid-tbl`、主場日曆 `.hc-*`、月曆批次輸入 `.vcal-*` |
| `<body>`（中段） | 七個分頁的 HTML 骨架 + 各種 modal overlay |
| `<script>`（後段，約佔全檔 2/3） | 全部邏輯 |

七個分頁：`schedule` / `preview` / `overview` / `analysis` / `homecal` / `setup` / `blackout`

找東西的可靠方法（檔案很長，且含 base64 LOGO 會干擾搜尋）：

```bash
grep -n "關鍵字1\|關鍵字2" index.html | grep -v base64 | head
```

### 關鍵函式

| 函式 | 做什麼 |
|------|--------|
| `checkConflicts()` | 四條衝突規則，回傳 issues 與要標紅的格子 |
| `orphanMatches()` / `renderOrphanWarn()` | 找出 key 不在目前格線上的比賽（改時段/縮賽季的後遺症），排程頁頂端紅字警示 |
| `cleanText()` | 隊名／場館／禁排原因的寫入端過濾（去掉 `< > "`），避免插進 template literal 時破版 |
| `safeSheetName()` | Excel 工作表名過濾＋去重（非法字元或同名會讓 ExcelJS 丟例外、匯出無聲失敗）|
| `teamStats()` | 每隊主客統計（平日/假日 + `byDay[0..6]` 各星期 + 一週三戰）|
| `renderFairness()` | 公平性檢視表（出賽日可設定，見第 5 節）|
| `renderMidSeason()` | 對戰分布（上下半季群聚 + 21 配對）|
| `sortedMatches()` | 依日期→時間排序並標全域場次號；匯出與對戰分布共用 |
| `exportExcel()` | 匯出總賽程活頁簿（總表 + 各隊分頁）|
| `renderHomeCal()` / `exportHomeCalExcel()` | 主場檔期日曆的畫面與匯出 |
| `homeCalDay(ds,opts)` | **日曆單日結果的唯一真實來源**，畫面與匯出共用 |
| `venueBlocked()` / `matchOffWhitelist()` | 白名單判定（見第 4 節）|
| `openModal()` / `confirmMatch()` / `removeMatch()` | 點格指派/確認/移除比賽 |
| `weekNoMap()` | WEEK 編號（只有排了比賽的週才編號、接續，聯盟實務勿改）|
| `schedTitle()` / `seasonPrefix()` | 匯出標題與賽季前綴（見第 6 節）|

---

## 4. 主場可用檔期白名單（核心機制）

`state.venueAvail` 是球團回報的可用主場日清單：

```js
{ teamId, date, time }   // time:'' ＝全天
```

### 判定邏輯

```
venueBlocked(teamId, date, time)
  該隊沒有任何白名單  → false（不限制）
  有白名單但該日未命中 → true（白名單外）
```

`matchOffWhitelist(date,time,homeId)` 就是包了一層的同一件事，語意是「這場比賽的主隊排在自己白名單外」。

### 設計意圖：白名單是唯讀的事實來源

**曾經有過「協調中／確定」的檔期狀態，已移除。** 舊版在白名單外排主場時會**反向補寫一筆 `tentative` 檔期**，問題是：排賽動作等於偷偷竄改球團回報的資料，補完之後那天就變成合法檔期，系統自己把自己的檢查解除了，事後無從分辨「球團真的說可以」與「我們硬排進去」。

現在的行為是：跳警示 →「仍要排入」→ 排入，**不阻擋、不寫入白名單**。

因此「白名單外」是**永久狀態**而非一次性提示。只要該日沒被補進白名單，該場次就會持續在排程格（`.slot.offwl` 橘色左框 + `.offwl-badge` 標籤）、預覽頁、進度總覽（專屬清單 + 計數）標示。**這是刻意的**——警示不是彈一次就算了，而是一份待辦追蹤。

### 舊存檔相容
`normalizeTeams()` 內有遷移：載入時剝掉殘留的 `status` 欄位，並依 `teamId|date|time` 去重（舊資料同一天可能同時存在 confirmed 與 tentative 兩筆，會收斂成一筆）。舊 JSON 可直接載入。

### 相關語意
**「清空賽程」不刪任何檔期。** 白名單是球團給的資料，只有比賽該被清掉。

---

## 5. 公平性檢視的出賽日（可設定）

`state.analysisDays`：`null`＝自動，陣列＝手動指定 `[0..6]`。

三層邏輯，由弱到強：

1. **自動（預設）**：欄位由賽程中實際出現的星期推導（`scheduledDays()`）。加了週四賽事，欄位自己長出來，不必改程式也不必記得去勾。
2. **完全沒排賽時**：退回 `FALLBACK_DAYS = [3,5,6,0]` 骨架，空表也有結構可看。
3. **手動覆蓋**：七個星期可勾選，覆蓋前兩者。

無論哪一層，只要有比賽落在沒顯示的星期，表格下方一律跳橘色警告，明講幾場、落在星期幾。

### 為什麼不用純勾選
純勾選的預設值仍得寫死在某處。一旦實際排賽日與預設不一致，就會回到「表面正常、實際少算」的問題，只是多了「要記得去勾」的手續——而人不會記得，畫面也不會抗議。

> 舊版曾寫死 `{3:'d3',5:'d5',6:'d6',0:'d0'}`，落在其他星期的比賽會被**靜默丟掉**：排在週四的比賽在表上完全不存在，但每列看起來都正常。這是修掉的實際缺陷，不只是擴充性考量。

`teamStats()` 的各日統計已改為 `byDay[0..6]={H,A}`，**統計層一律七天全記**，顯示哪幾欄純由 UI 決定。日後加星期不必動統計層。

`一週三戰次數` **刻意不跟著勾選變動**——它算的是整週實際出賽密度，與想看哪幾欄無關。

---

## 6. Excel 匯出

### 版面規格（以使用者提供的 V4 樣張為準）

| 項目 | 規格 |
|------|------|
| 欄位 | `場次/週次/日期/星期/時間/主場/客場` 共 7 欄（**無狀態欄**）|
| 欄寬 | `[6,6,13,6,8,13,13]` |
| 字型 | 微軟正黑體；標題 14 粗、其餘 11 |
| 標題列 / 表頭列 | 藍底 `#0070C0` + 白色粗體 |
| 資料列 | 黑色細框線、水平垂直置中 |
| 主客場欄 | 填該隊主色，字色依亮度自動配黑白（`paint()`）|
| 標題列高 | 21；凍結窗格 A3；標題與表頭為連續兩列 |

### 隊伍分頁是三區塊「橫向並排」

| 欄 | 內容 |
|---|---|
| A–G | 完整總賽程 |
| I–O | 該隊主場賽事 |
| Q–W | 該隊全部賽事 |

中間各空一欄（寬 3）。合併範圍 `A1:G1` / `I1:O1` / `Q1:W1`。

`writeBlock(ws, col0, title, rows)` 用**絕對列/欄定位**而非 `addRow`，才能把多個區塊並排在同一張表上。`col0` 是該區塊最左欄（1-based）。改版面時注意這點。

### 標題與檔名
- `state.schedTitle`：頂欄「賽程表名」輸入框，存進 JSON。
- `seasonPrefix()`：把表名尾端的「總賽程／賽程表／總表／賽程」連同版本號切掉，當隊伍分頁區塊標題的前綴。
  - `2026-27 TPBL 總賽程 V4` → `2026-27 TPBL`
  - 找不到關鍵字 → 原樣使用；切完為空 → 退回完整表名。
  - **版本號要放尾端**，寫在前面會被一起當成前綴。

### 列印 / 存成 PDF（`printCurrentView()`）

走**瀏覽器原生列印**，沒有外掛截圖函式庫。曾評估 html2canvas + jsPDF，不採用的理由：產出是點陣圖（放大糊、檔案大）、分頁得自己算且常把整週卡片攔腰切斷、對某些 CSS 支援不穩，還多兩個 CDN 相依。原生列印是向量文字、可搜尋可選取，分頁交給 `break-inside:avoid` 由瀏覽器處理。

**列印樣式刻意綁 `.view.active`，不寫死分頁**：

```css
.view{display:none !important}
.view.active{display:block !important}
```

這樣「印你現在看的那一頁」，不必為每個分頁各寫規則。早期版本曾寫死 `.view#view-homecal{display:block}`，副作用是列印**任何**分頁都會被強制換成日曆——別再那樣寫。

兩個必要細節：
- `print-color-adjust:exact` — 瀏覽器列印預設濾掉背景色，隊色/衝突紅底/禁排底色是這份表的重點，濾掉就等於作廢。
- `.print-head` — 頂欄在列印時隱藏，所以標題與產出時間要另外注入內容區；螢幕上以 `display:none` 藏起來。

### 主場檔期日曆匯出
結構：每月一塊 → 月標題 → 星期表頭（一～日）→ 每週「一列日期 + 每隊一列」。

**為什麼一隊佔一格**：Excel 一格只能有一個底色，擠在同格就只剩純文字，隊色辨識全失。每週的隊伍列數取該週最多者，不灌空列。

匯出內容跟隨畫面篩選（`homeCalOpts()`），副標題註明目前條件，避免收件者誤以為是全量資料。

---

## 7. 兩個 Excel 函式庫（重要）

工具**同時**用了兩個，各司其職，不要合併：

- **SheetJS（`XLSX`）** — 負責「讀」：解析球團上傳的可用檔期、產生填寫模板（`downloadVenueTemplate()`）。
- **ExcelJS** — 負責「寫」：匯出賽程與日曆，因為要**儲存格底色**（球隊主色），而 SheetJS 社群版不支援。

兩者都從 Cloudflare CDN 載入、版本寫死在 `<head>`。因此：

- **要連網**才能用上傳/匯出（排程本身可離線）。
- 若要完全離線：把兩個 `.js` 下載進 repo（例如 `lib/`），把 `<head>` 的兩個 `<script src>` 改成相對路徑。代價是升版要自己手動換。

---

## 8. 常見修改點

| 想改什麼 | 去哪改 |
|---|---|
| 衝突規則 | `checkConflicts()`，四條規則各自獨立，照樣式加即可 |
| 公平性統計欄位 | `teamStats()` 加欄位 → `renderFairness()` 加表頭與 `cell()`。**加星期不必動統計層**（已七天全記）|
| 出賽時段 | 「隊伍與賽季」頁可增刪，或改 `DEFAULT_SLOTS`（`day` 用 `0=日…6=六`）|
| 匯出格式 | `exportExcel()`。上色在 `paint()`、格線在 `bd`、區塊由 `writeBlock(ws,col0,title,rows)` 產生 |
| 日曆邏輯 | **改 `homeCalDay()`**，不要在 render 或 export 裡各自加判斷 |
| 隊伍/隊色 | 「隊伍與賽季」頁即可，或改 `DEFAULT_TEAMS` |
| 比賽備註顯示位置 | 排程格 `.mnote`、賽程預覽 `.pv-note`。**新增顯示處記得用 `esc()` 逸出**（自由文字含 `<`、`"` 會弄壞版面）|
| 新增分頁 | ① `<button class="tab" data-tab="xxx">` ② `<div class="view" id="view-xxx">` ③ **`switchTab()` 與 `renderAll()` 兩張分派表都要加** |

---

## 9. 踩過的坑

### CSS 權重（會再踩）
基底規則是：

```css
table.tbl th, table.tbl td { text-align:left }   /* 權重 (0,1,2) */
```

`.mid-tbl td { text-align:center }` 權重只有 (0,1,1)，**會被基底蓋掉**。這條規則其實從一開始就沒生效，只是 `.mid-list` 有 `!important` 撐著才沒被發現；拿掉 `!important` 後才露餡。

**新增表格對齊規則時一律用 `table.tbl.xxx` 前綴**：

```css
table.tbl.mid-tbl td { text-align:center }
table.tbl.mid-tbl td.midrowh { text-align:left }
table.tbl.fair-tbl th { text-align:center }
table.tbl.fair-tbl th.fcol-team { text-align:left }
```

CSS 內已留註解說明。**肉眼審查抓不到這類問題**，一定要用 jsdom 驗（見第 10 節）。

### ExcelJS 不能在 `vm.runInContext` 裡測
跨 realm 的 Array 會讓 ExcelJS `addRow` **靜默失效**——產出的檔案有完整樣式但所有儲存格都沒有值，而且不會報錯。抽出來測時要用 `new Function(...)` 在主 realm 執行。這個浪費過一次除錯時間。

### 季中切分用日期、不用場次
「對戰分布」是用**日期**切上下半季，不是用場次編號。群聚是「行事曆體感／賣票」問題，用日期切才對應觀眾感受。另外，各隊上下半季主場**總數**本就不會剛好一半一半（受場館檔期影響），那是雜訊；真正要抓的是**單一對手 3-0／0-3 的集中**。

### 兩張分派表不同步
見第 2 節。新增分頁只加一邊，症狀是「點進去空白」或「改了不刷新」，兩種都發生過。

### 分頁「切過去才重畫」
偶爾造成「我明明改了怎麼沒更新」的錯覺——切一下分頁即可。這是刻意設計。

---

## 10. 驗證與測試習慣

檔案已約 3,280 行，改完務必跑。

**JS 語法**（不執行）：
```bash
node -e 'const fs=require("fs"),vm=require("vm");
[...fs.readFileSync("index.html","utf8").matchAll(/<script(?![^>]*src=)[^>]*>([\s\S]*?)<\/script>/g)]
  .forEach(x=>{try{new vm.Script(x[1]);console.log("JS OK")}catch(e){console.log("ERR",e.message)}});'
```

**CSS 權重**：用 jsdom 載入 `<style>`，對代表性元素斷言 `getComputedStyle(...).textAlign`。這是唯一能可靠抓到第 9 節那類問題的方法。

**Excel 匯出**：把 `exportExcel()` 抽出在 Node **主 realm** 執行，再用 openpyxl 回讀比對合併範圍、字型、填滿色、對齊、框線、欄寬（round-trip 測試）。

**功能邏輯**：把目標函式區段用 `new Function(...)` 抽出，餵假 `state` 與最小 DOM stub 斷言。日曆、公平性出賽日、白名單外流程、清除客隊、舊資料遷移都建過這類測試。

---

## 11. 已知限制 / 待辦

- 單檔已約 3,280 行，只會更長。**目前還撐得住，但若維護開始痛，值得考慮拆成 `index.html` + `style.css` + `app.js`**（GitHub Pages 一樣支援，只是多兩個檔）。
- 白名單外的比賽只有視覺標示，沒有「批次把這些日期補進白名單」的捷徑；場次多時會是重複勞動。
- 主場檔期日曆是唯讀檢視，不能直接在上面排賽或編輯檔期。
- **禁排日會蓋掉日曆的檔期顯示**：某隊該日有回報檔期、但當天是全聯盟禁排時，格子只顯示「🚫 原因」不列隊伍。那天本來就不能排，屬刻意行為；若要改成兩者並存，畫面與匯出（`homeCalDay()`）要一起改。
- **未上傳檔期的隊伍在日曆上預設不列**：他們主場不受限制、每天都能排，全列出來會灌爆版面。需要時勾「一併列出」，以虛線白底 chip 區隔。屬刻意行為。
- 對戰分布/公平性只計**已排**比賽；未排滿時數字反映當下進度，不是最終值。
- **比賽備註目前不進 Excel 匯出**：總賽程是刻意對齊 V4 樣張的 7 欄格式，加欄會破壞版面。若要匯出，建議新增第 8 欄「備註」並同步調整 `COLW`、`HEAD`、`NCOL` 與三區塊並排的位移量 `step`。

---

## 12. 交接檢查清單

- [ ] 拿到最新 JSON 進度檔（不是只靠某台電腦的 localStorage）
- [ ] 確認 GitHub repo 存取權與 Pages 設定
- [ ] 知道主場骨架與客隊指派都在本工具內完成（不是外部來源），以及目前排到哪一階段
- [ ] 知道各球團檔期由誰對接、更新頻率
- [ ] 讀過第 4 節（白名單為何是唯讀）與第 9 節（CSS 權重、ExcelJS 測試陷阱）
- [ ] 本機能跑起 `index.html`、能匯出總賽程 Excel、能匯出日曆 Excel、能載入 JSON

---

*本文與 `README.md` 一併維護；工具行為若有變動，記得回來更新對應段落。*
